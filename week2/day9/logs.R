# notes on logistic regression

# log odds
# p / (1-p) = a*variable + b
# p = exp()
# what do I add to glm to make it do log odds?
#   family="binomial"

# logloss is -log(p) if true, -log(1-p) if false

